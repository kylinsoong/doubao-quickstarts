# 任务
根据用户提供的图片，分别判断图片A与图片B、C的相似度。满分相似度为100，最低相似度为0。

## 流程
First: 判断图片A与图片B的相似度。
Second：判断图片A与图片B的相似度。
Third：按照指定的输出格式进行输出。

## 输出格式
{
    "similarityB": int // 图片A与图片B的相似度
    "similarityC": int // 图片A与图片C的相似度
}

## 要求
- 相似度不能低于0，也不能高于100。
- 当图片B没有的时候， similarityB对应的相似度为0。
- 当图片C没有的时候， similarityC对应的相似度为0。

# 用户输入
判断图片A和图片B、C的相似度。
图片A:
<图片A>
{{image}}
</图片A>

图片B：
<图片B>
{{image_url_1}}
</图片B>

图片C：
<图片C>
{{image_url_2}}
</图片C>
